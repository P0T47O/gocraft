# 平滑光照

## 检查结论

旧版 `applyAOSmooth` 只对 AO 和群系颜色做逐顶点处理，面光照仍是一个 `byte`，四个顶点共用。因此名字里的 Smooth 并不代表真正的逐顶点光照。四边形还固定采用 0–2 对角线，遇到不对称的角落阴影容易出现三角形痕迹。

## 当前实现

- `world_light.go` 保留天空光/方块光传播；网格仍读取不可变的一格边缘快照，不新增渲染线程同步或世界访问。
- `mesh_lighting.go` 用统一方向表描述六面法线、切向量及顶点顺序，每个可见面读取外侧 3×3 网格。每个角点平均与之相接的四格有效光照，相邻共面方块共享角点采样。
- 不把不透明格子的零光照计入平均，避免额外压暗；两侧同时被不透明方块遮挡时，不采被封住的对角格，避免拐角漏光。
- AO 独立计算接触遮挡，再与顶点亮度相乘。玻璃和冰不应用 AO；自发光方块保留亮度下限，避免熔岩被方向色和 AO 压暗。水的透明度不随光照变化。
- `addFaceSmooth` 按最终顶点亮度选择 0–2 或 1–3 对角线，保持顶点绕序、UV、法线不变。
- 草侧覆盖层复用底层面的采样结果，不重复读取邻格。

## 范围与限制

此次改善普通立方体面（包括叶块、玻璃、水和熔岩）的光照。交叉植物、火把模型和缩进仙人掌仍使用原有简化着色，未改成完整逐顶点模型光照。天空光与方块光在网格快照中仍取最大值，不是双色光图，也不包含动态日夜光色、阴影贴图或全局光照。保留原有最低 10% 环境亮度曲线。

## 验证

- `mesh_lighting_test.go`：六面顶点顺序与梯度、正负区块/分段边界连续性、封闭拐角防漏光、透明度与自发光、对角线切换后的绕序。
- `GOCRAFT_LIGHT_PREVIEW=1 go test . -run TestSmoothLightingPreview`（PowerShell 先设置同名环境变量）：隐藏窗口使用实际顶点格式和 shader 渲染对照，读取像素验证插值，输出 `work/smooth-light-comparison.png`。左侧是原来的单面亮度，右侧为逐顶点亮度；这是去掉材质和雾的合成测试，不是游戏场景截图。
- 系统 Go 1.27.1、i7-14700HX 上，已有 `BenchmarkMeshTintCache/cached` 改前约 137 µs、改后约 135 µs，均约 521 allocations/op；该简单场景未见明显回退，但不代表所有地形的性能。
