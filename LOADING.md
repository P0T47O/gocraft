# 区块加载与性能诊断

## 调度与线程

- 服务端玩法仍为 20 TPS；加载在同一个 owner 循环内，每 8 ms 获得一次处理机会。没有新增并发修改 World 的线程。
- 每次接收生成结果最多使用 3 ms、发送准备最多使用 2 ms；批量数随积压在 4–32 之间变化。一项光照拼接或快照操作不可中途切断，因此预算不是严格实时上限。
- 待发送区块按距离排序，每 50 ms 重算一次，避免每个加载周期都对几千个坐标重新排序。新请求或移动导致的优先级变化最多延迟一个重排周期。
- 发送队列达到 50% 时暂停该客户端的区块发送，为玩法消息保留至少半个队列；128 槽默认队列因此可容纳一次 36 槽完整背包同步并保留额外余量。其它客户端不受它阻塞。客户端收包队列限制为 128，最终由 TCP 反压传导至服务端。不会无限扩大队列来追求吞吐。
- 单人暂停时暂停加载处理；生成 worker 可完成已经领取的工作，并在有界结果队列处等待。

## 减少重复工作

- 客户端按距离建立请求列表，在跨区块移动或视距变化时重建；每次调用最多检查 256 个坐标、发送 32 个请求，游标跨帧保留。遍历完成后最多每秒开始一次补漏扫描，单坐标保持 3 秒重试间隔。扫描较大范围本身可能跨越多帧，因此重试不是严格 3 秒定时器。
- 请求队列达到半满时保留游标并暂停发送，不再让可重试请求触发普通玩法发包的满队列断线策略。离开请求半径和退出会话时清理请求计时，避免长距离移动无限积累。

- 一个生成任务共享 24×24 世界坐标环境样本网格，覆盖 16×16 区块、树冠边缘与坡度中心差分。地表与树木复用样本，不改变随机数、噪声公式或放置顺序。
- 接收区块时记录精确的一格采样边缘，只标记会受影响的邻居/高度分段。重复更新合并到待处理网格；运行中的旧快照只失效一次，新快照仍会读取最新数据。
- 光照刷新以 16 格高的分段发送。已知区块只传光照，首次加载和显式重请求仍传完整快照；完整请求优先于增量。TCP 顺序保证初始化先于增量，客户端忽略已卸载区块的增量。
- 网络协议版本为 **5**，联机双方需要更新；存档和地形格式未改变。

## 日志

`performance.log` 保留原有帧耗时、网格队列等列，并新增：

| 阶段 | 含义 |
| --- | --- |
| DiskLoad | 区块存档读取尝试，包括未找到存档 |
| Generation | 无存档时生成方块、矿石与植被 |
| LightInit / LightStitch | worker 内初始化 / owner 上的邻居光照拼接 |
| Snapshot | 完整或光照分段快照构建 |
| NetworkWrite | 区块包编码与 socket 写入等待，不是网络 RTT |
| ClientApply | 客户端应用完整/光照快照 |
| MeshCPU | worker 构建网格 |
| Upload | CPU 网格转换、旧网格释放与 GPU 上传调用 |

每阶段的 `WorkMS` 为本次日志间隔内累计工作墙钟时间，`Calls` 为次数。多个 worker 的时间相加，不能直接当作一帧延迟或端到端延迟。

`GenerationQueued/GenerationReady/ChunksPending/ClientPacketsQueued` 表示积压；`ChunksPublished` 是服务端完成接收的数量；`MeshStale` 是被丢弃的网格结果数。`ClientChunksReceived/s` 只统计客户端首次接收，避免原来单人模式将服务端和客户端重复计算。

## 验证与复现

### 客户端端到端观测（新增）

跨区块移动或传送但视距不变时，现在直接平移已排序请求坐标，保持近处优先顺序及底层切片；只有视距变化时重新生成并排序。主循环的请求时钟通过 `requestMissingChunksAt` 显式传入以测试重试边界，正式入口仍使用真实时间。

2026-09-22，i7-14700HX：`go test . -run '^$' -bench BenchmarkChunkRequestPlanMovement -benchmem -benchtime=200ms`，平移/重建排序对照：16 半径约 0.52µs/176.6µs，32 半径约 2.08µs/1012.4µs，64 半径约 8.89µs/5060.7µs；平移 0 分配。该基准只测请求列表移动更新，不包含区块查找、网络、生成、网格或 GPU，也不是完整加载路线基准。

`RequestReceiveCount/P95MS/P99MS` 记录首次成功入队请求到完整区块应用完毕的时间；`RequestReadyCount/P95MS/P99MS` 记录首次请求到所有非空分段网格完成 CPU 上传调用的时间。重试不重置首次时间，重复完整回包不重复计数。空区块在接收时即可就绪；后续光照/编辑重建不重复计数。每个日志间隔统计已完成样本，未完成/离开范围的请求不混入分位数，因此同时查看 `ClientRequestsPending/ClientReadyPending`，不能只看已完成样本。

Ready **不是** GPU 时间戳、屏幕呈现完成时间或固定视距全部加载完成时间。服务器主动发送而客户端未请求的区块没有这两类样本。暂停时间包含在墙钟延迟中。

`WorldMeshBufferBytes` 是当前成功分配的世界网格顶点/索引缓冲区字节数；`WorldMeshUploadedBytes` 是日志间隔内成功上传的世界网格字节数。采用实际 24/36 字节顶点格式；均不含图集、UI、实体动态缓冲区、驱动和暂存内存，不能当作总显存。`ActiveMeshes` 现由 WebGPU 成功上传/释放维护。

对照流程：固定种子 12345、同一出生位置与路线，分别选择 16/32/64 视距；冷加载和已有存档加载分开，每组至少三次。记录首次可操作时间、可见地形完成时间、FrameP95/P99、上述延迟/积压与上传量；保留每次 performance.log（下一次启动会覆盖）。尚未完成该整机对照，不以服务端测试或队列微基准宣称整机加速。

请求队列回归：`go test . -run 'TestChunkRequestPlan|TestChunkStreamingBackpressure|TestChunkReadyLatency'`。静止状态微基准：`go test . -run '^$' -bench BenchmarkChunkRequestPlanStationary -benchmem`；仅测列表无需更新时的检查开销，不代表区块加载速度。

现阶段仍保留网格处理 2 ms / 4 MiB 的原预算（字节预算是 CPU 网格估算，非实际 GPU 上传计数），暂不引入自适应预算。GPU 时间戳与自动化整机固定路线基准仍待补充。

使用环境中的 `go`：

```powershell
go test ./...
go run ./tests/generation
go test . -run '^$' -bench 'BenchmarkGenerationSampling' -benchmem -count=2
$env:GOCRAFT_STREAM_LOAD_TEST='1'
go test . -run '^TestStreamingRadius32ColdLoad$' -v -count=1 -timeout=120s
Remove-Item Env:GOCRAFT_STREAM_LOAD_TEST
```

冷加载测试不读取或写入存档，不创建 GL 窗口，会临时分配约数千个区块。它测量服务端生成、光照和快照进入模拟发送队列的时间，**不含真实 TCP、客户端网格和 GPU 渲染**。

2026-09-15，i7-14700HX / 系统 Go 1.27.1：

- 多坐标生成对照：原路径约 2.83–3.25 ms/块，缓存路径约 1.55–1.64 ms/块；测试验证方块和高度逐字节一致。
- seed 12345、32 半径、3209 区块：服务端冷加载测试约 3.34 s（961 块/s）。原实现仅接收/发送调度的理论下限为 3209÷80≈40.1 s；没有把理论下限当作旧版本整机实测。
- 游戏内加载完成时间仍需实机对照；网格构建和上传仍保留每帧预算，不能用上述数字替代视觉完成时间。
