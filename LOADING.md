# 区块加载与性能诊断

## 调度与线程

- 服务端玩法仍为 20 TPS；加载在同一个 owner 循环内，每 8 ms 获得一次处理机会。没有新增并发修改 World 的线程。
- 每次接收生成结果最多使用 3 ms、发送准备最多使用 2 ms；批量数随积压在 4–32 之间变化。一项光照拼接或快照操作不可中途切断，因此预算不是严格实时上限。
- 待发送区块按距离排序，每 50 ms 重算一次，避免每个加载周期都对几千个坐标重新排序。新请求或移动导致的优先级变化最多延迟一个重排周期。
- 发送队列达到 75% 时暂停该客户端的区块发送，为玩法消息留空间；其它客户端不受它阻塞。客户端收包队列限制为 128，最终由 TCP 反压传导至服务端。不会无限扩大队列来追求吞吐。
- 单人暂停时暂停加载处理；生成 worker 可完成已经领取的工作，并在有界结果队列处等待。

## 减少重复工作

- 一个生成任务共享 24×24 世界坐标环境样本网格，覆盖 16×16 区块、树冠边缘与坡度中心差分。地表与树木复用样本，不改变随机数、噪声公式或放置顺序。
- 接收区块时记录精确的一格采样边缘，只标记会受影响的邻居/高度分段。重复更新合并到待处理网格；运行中的旧快照只失效一次，新快照仍会读取最新数据。
- 光照刷新以 16 格高的分段发送。已知区块只传光照，首次加载和显式重请求仍传完整快照；完整请求优先于增量。TCP 顺序保证初始化先于增量，客户端忽略已卸载区块的增量。
- 网络协议版本为 **5**，联机双方需要更新；存档和地形格式未改变。

## 日志

`performance.log` 保留原有帧耗时、网格队列等列，并新增：

| 阶段 | 含义 |
| --- | --- |
| DiskLoad | 区块存档读取尝试，包括未找到存档 |
| Generation | 无存档时生成方块、矿石与植被 |
| LightInit / LightStitch | worker 内初始化 / owner 上的邻居光照拼接 |
| Snapshot | 完整或光照分段快照构建 |
| NetworkWrite | 区块包编码与 socket 写入等待，不是网络 RTT |
| ClientApply | 客户端应用完整/光照快照 |
| MeshCPU | worker 构建网格 |
| Upload | CPU 网格转换、旧网格释放与 GPU 上传调用 |

每阶段的 `WorkMS` 为本次日志间隔内累计工作墙钟时间，`Calls` 为次数。多个 worker 的时间相加，不能直接当作一帧延迟或端到端延迟。

`GenerationQueued/GenerationReady/ChunksPending/ClientPacketsQueued` 表示积压；`ChunksPublished` 是服务端完成接收的数量；`MeshStale` 是被丢弃的网格结果数。`ClientChunksReceived/s` 只统计客户端首次接收，避免原来单人模式将服务端和客户端重复计算。

## 验证与复现

使用环境中的 `go`：

```powershell
go test ./...
go run ./tests/generation
go test . -run '^$' -bench 'BenchmarkGenerationSampling' -benchmem -count=2
$env:GOCRAFT_STREAM_LOAD_TEST='1'
go test . -run '^TestStreamingRadius32ColdLoad$' -v -count=1 -timeout=120s
Remove-Item Env:GOCRAFT_STREAM_LOAD_TEST
```

冷加载测试不读取或写入存档，不创建 GL 窗口，会临时分配约数千个区块。它测量服务端生成、光照和快照进入模拟发送队列的时间，**不含真实 TCP、客户端网格和 GPU 渲染**。

2026-09-15，i7-14700HX / 系统 Go 1.27.1：

- 多坐标生成对照：原路径约 2.83–3.25 ms/块，缓存路径约 1.55–1.64 ms/块；测试验证方块和高度逐字节一致。
- seed 12345、32 半径、3209 区块：服务端冷加载测试约 3.34 s（961 块/s）。原实现仅接收/发送调度的理论下限为 3209÷80≈40.1 s；没有把理论下限当作旧版本整机实测。
- 游戏内加载完成时间仍需实机对照；网格构建和上传仍保留每帧预算，不能用上述数字替代视觉完成时间。
