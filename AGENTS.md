# 项目开发默认指引

## 功能索引必须随代码维护

- 开始定位功能时先阅读 `CODE_INDEX.md`，按符号查找时使用 `CODE_SYMBOLS.md`。
- 新增功能、移动文件或调整职责时，在同一批修改中更新 `CODE_INDEX.md` 的功能映射、入口和测试说明。
- 新增、删除或移动 Go 类型/函数后运行 `go run ./tools/codeindex -write` 更新自动符号索引，不手工编辑 `CODE_SYMBOLS.md`。
- 交付或提交前运行 `go run ./tools/codeindex -check`，保证索引不过期，功能索引中的文件链接仍有效。
- 文件拆分应按职责进行，保留可读的调用链；未完成的结构改进应在索引中明确记录。

## 验证与范围

- 常规验证：`go test ./...`、`go vet ./...`、`go build .`。
- 修改地形生成时额外运行 `go run ./tests/generation`；视觉结论不能仅由测试通过推断，应注明是否实际运行验证。
- 活跃 World 由所属主循环持有；生成线程操作私有区块，网格线程读取快照。GPU 操作留在渲染线程。
- 保留用户存档、材质资源和本地设置；不要无关地提交 `settings.json`、生成的可执行文件或诊断图片。
