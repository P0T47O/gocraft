# 稀疏区块存储：实现与测量

## 实现

每个Chunk的blocks/meta/skyLight/blockLight由固定16×256×16字节数组改为16个16×16×16段。每段要么仅保存一个统一值，要么独占4096字节数组。零值即全空气/全零，无需分配；更改统一段中一个格子时展开并保留其它4095格。生成、初始化光照、完整收包和光照增量的批量边界折叠统一段，不在每次单格写入后扫描压缩。

天空光初始化采用统一15起步，再将被遮挡部分写为0；空天空不分配数组。首次网络快照按段批量导入，避免旧版本的逐格读取/展开/再次压缩。未引入调色板或4位打包；不同字段仍独立，便于保持现有光照逻辑。

读写继续遵守原Chunk线程/锁归属。非空chunkPlane包含指针，不能把它当值数组复制后同时修改；需要独立副本时使用Clone。光照重建保存的旧平面只读，新初始化先整体替换，旧平面不会被新光照写入。网格worker仍使用原有密集快照，未直接读取可变稀疏段。

存档和网络字节顺序、协议版本保持不变。存档写入已改为直接遍历稀疏段输出兼容的调色板/RLE，不再密集展开；读取暂时仍使用短生命周期密集适配器，每块额外分配两份64KiB缓冲并有转换成本。

## 正确性

- 迁移前记录3种子×4坐标的方块、元数据、天空光、方块光逐字节SHA-256：`6a03ca4e703e039da1917ef7045d0d5e4bd4559ef1f3511e1555ed89e0719b4d`；迁移后匹配。
- 随机10万次写入、统一值展开/回收、克隆隔离、网络步长和半字节解码测试。
- 相同地形16个分段的密集/稀疏数据源产生完全相同的网格结果。
- 原有存档往返、截断保护、跨块光照及独立地形测试通过。

## 微基准（i7-14700HX，单机两次短测）

| 项目 | 原密集存储 | 稀疏存储 | 解释 |
| --- | --- | --- | --- |
| 固定64块地形样本：Chunk结构+体素数组平均 | 263224 B | 25144 B | 约减少90.4%，不含切片/map背后内存；样本最大30776 B |
| 空Chunk结构 | 263224 B | 2104 B | 尚未分配的服务端占位也受益 |
| 生成同一区块 | 1.656–1.659 ms | 1.705–1.721 ms | 约慢3%–4%；每次分配约28KB/5次→47KB/10次 |
| 扫描四字段全部体素 | 53.9–54.4 µs | 93.9–96.6 µs | 约慢74%–79%；随机访问增加段索引和条件分支 |
| 空区块光照初始化 | 125.7–127.4 µs | 123.8–125.6 µs | 基本持平，均0分配 |
| 相同地形直接数据源网格构建 | 1.440–1.451 ms | 1.512–1.546 ms | 约慢4%–7%，分配次数相同；正式worker用密集快照，不应直接把此比例当作实机网格退化 |

命令：`go test . -run 'TestChunkPlane|TestChunkStorage' -v`；`go test . -run '^$' -bench 'BenchmarkChunkGeneration$|BenchmarkChunkStorageScan$|BenchmarkLightingInitializeEmpty$|BenchmarkChunkStorageMesh' -benchmem -count=2`。

## 128范围实机

对照是已含512请求窗口/动态收包预算的密集版本，不是最初无限积压版本：密集版本17.18秒收到9435块、堆6876MiB，触及6GiB保护停止。

第一版稀疏存储（尚未优化首次收包导入）完成45.03秒时间上限，没有触发内存保护：第44.5秒收到16920块、Go堆1221MiB。约9524块时堆870MiB；和密集版本相近区块数量相比明显下降，但GC时间和世界内容有差异，不能当作严格存活堆比值。代价是前10秒仅收到4147块，对照密集版本5617块，吞吐约下降26%。

随后增加首次收包按段批量导入，正确性与全量测试通过；最终版128复测在0.06秒、堆约30MiB时因系统可用物理内存不足4GiB提前退出（检查时系统约1.1GiB可用），因此**尚无最终批量导入版的有效128吞吐对照**。没有关闭其他应用或绕过内存保护。

## 结论与后续

### 直接存档编码优化

新增直接稀疏段编码路径，使用256项固定索引表代替map，并边遍历边输出RLE。保留原调色板首次出现顺序、x/y/z字节顺序、65535最大游程和可选元数据语义。全零、统一、随机全ID范围和真实地形均与原编码器逐字节一致。

`BenchmarkSparseSaveEncoding` 两次短测：此前密集适配路径1.389–1.476ms、约262KB/块、8次分配；直接路径0.114–0.128ms、8480B/块、13次分配。编码约快11–13倍、分配字节减少约97%，但小分配次数增加；不包含zstd压缩、文件落盘或整个世界保存耗时，不能据此声称总保存速度提高同等倍数。

### 批量读取后续优化

首次完整区块收包在已有数据包扫描中同步计算高度、非空方块计数和火把位置，去掉随后两遍稀疏存储扫描。网格快照按垂直16格段解析一次表示形式，连续复制整列；方块光为统一0时直接跳过合并。线程归属、缺失邻居默认光照和快照布局不变。列步长/跨段/光照最大值合并及首次收包派生元数据都有等价测试。

同种子128短跑：约10秒收到4655块，上一轮4067块，增加约14%；约9523块用时21.2秒（上一轮9394块约24.2秒，密集版本9435块约17.2秒）。因此部分追回吞吐，但仍慢于原密集存储，不把单次短跑当统计显著结论。

本轮40.51秒收到15933块、Go堆约1088MiB时因系统可用物理内存保护提前结束，非6GiB堆阈值；正常完成保存退出。此结果不可作为45秒完成数直接与上一轮比较。全量回归、静态检查和构建通过。

### 最终版复测补充（释放后台应用后，2026-09-22）

系统起始可用物理内存约8.8GiB，最终批量导入版成功跑满45.01秒并正常保存退出，未触发内存保护：收到16242块，结束时Go堆1232MiB，1–44.9秒采样峰值1424MiB。44.5秒有16083块、堆927MiB；24.2秒有9394块、堆603MiB。对照密集版本17.18秒9435块、堆6876MiB：相近数量下内存大幅下降，但加载时间约增加41%（吞吐约下降29%），仅为两次短跑，含GC与系统负载差异。

该次记录的分段FPS平均约44.5，末段29FPS，末段帧P95/P99约45.1/47.1ms；平均值不是整场统一帧样本分位数。完整128范围仍未加载完。最终批量导入未在本次实测中证明恢复密集版本吞吐，不能宣称又省内存又提速。上文因低可用内存未完成最终版复测的限制已由本次补测解除。

在样本及第一版实机中，常驻内存收益显著；代价是读访问分支、小段分配、批量压缩与存档转换成本。最坏情况下所有段都非统一，体素数组仍占256KiB，外加约1KiB段描述开销；收益依赖空气/光照/元数据的统一程度，不保证所有世界都节省90%。

保留这次改造，但不宣称最终版整机“又省又快”。下一步优化快照按段批量复制、直接分段存档编码和批量字段读取，并单独剖析后段绘制与主线程遍历；避免为节省内存引入过多逐体素调用。
