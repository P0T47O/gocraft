# 128 区块半径诊断（2026-09-22）

## 方法与限制

RTX 5060 / Vulkan，原生隐藏窗口1280×720，seed 12345，临时新世界，真实本机 TCP、正式 updateGame、网格 worker 和 WebGPU 绘制；固定高度143的俯视相机，不覆盖个人设置、存档或 performance.log。开跑时系统约17GiB可用物理内存，保护条件为45秒或Go堆超过6GiB或剩余物理内存低于4GiB。检查每秒一次，因此阈值可短暂超出。当前结果不是完整128范围加载或长时间稳定性验收，也不是GPU时间戳测量。

运行：`$env:GOCRAFT_STRESS128='1'; go test . -run '^TestStreaming128Stress$' -v -count=1 -timeout=180s`。输出至忽略目录 `work/stress128.csv` 和 `work/stress128.cpu`；再次运行会覆盖这两个诊断文件。CPU剖析包含退出保存，CSV不含退出保存。测试结束会正常保存临时世界并清理。不要与其他大内存游戏会话同时运行。

## 本次短跑

- 10.08秒达到堆保护停止，测试及退出保存成功；Go堆约6537MiB。
- 客户端收到2777块，未完成请求16071个；服务端待发约15909个。
- 末段仍约58–59 FPS、277–278新块/秒，客户端收包队列恒为128。未观察到完全停滞。
- 第10秒服务端生成1243块，客户端仅应用277块；生成明显跑在接收前面。
- 第10秒 ClientApply累计125.6ms、MeshCPU累计62.9ms、Upload累计5.0ms。多线程累计时间不能直接相加当作一帧耗时。
- 世界网格缓冲区约85MiB（不含纹理、实体、驱动分配等），与6.4GiB堆相比不是主要内存来源。
- CPU样本中 caveNoise直接占19.6%、含下级调用约30.4%；这是生成CPU热点，不等于整个流水线的吞吐瓶颈。

## 用户已有188秒日志交叉验证

只读分析原 performance.log 最后30条：平均20.5 FPS、79新块/秒，收包队列平均127.2/128；生成调用为0，MeshCPU累计平均3.62ms/日志间隔、Upload平均2.80ms。最后客户端26575块、服务端仍待发24787块，Go堆30660MiB，世界网格缓冲区约464MiB，约10247次世界绘制。

该日志后段不是“完全不加载”，而是吞吐降到约每秒几十块。在约2.5万块未完成时，仅剩余发送/接收便需要数分钟；后续帧率下降或重试还可能延长等待。不能用此次短跑证明其后段帧率下降究竟由GPU、绘制提交、地图遍历还是系统换页主导；还需该阶段CPU/GPU及系统内存采样。

## 优先短板与依据

1. **客户端收包处理与帧率绑定。** updateGame每帧仅给收包应用2ms；60FPS约120ms/秒预算，20FPS只剩40ms/秒。实测短跑125.6ms/秒和长日志41.3ms/秒接近这一预算。约0.45–0.52ms/块的应用成本让吞吐随帧率掉到约80块/秒，满收包队列又反压TCP。优先做时间维度的吞吐预算/安全流水线分离，不能简单把单帧预算无限放大。
2. **只有发包队列反压，没有在途请求总量窗口。** 每帧可发32请求，在60FPS下远超约280块/秒的消费；服务端 requestChunk 收到请求就分配完整Chunk占位，尚未生成也占内存。应限制未完成请求数并按接收进度补充，再考虑服务端延迟实体化占位。
3. **区块存储常驻成本高。** 一个Chunk仅blocks/meta/skyLight/blockLight四个256高数组就有256KiB；128半径约5.1万块，单份约12.6GiB，单人服务端和客户端两份约25GiB，尚未计入网格、快照、容器及运行时。应优先稀疏分段、缺省光照/元数据及共享/压缩存储设计，而不是继续加队列。
4. **大视距绘制和每帧遍历。** 长日志超过一万次世界绘制，updateGame还每帧遍历卸载检查；它们是待剖析候选，不把未测得的GPU耗时当作已确认原因。后续做稳定场景CPU/GPU分段计时，再决定批处理、分配池、LOD。

本次只增加可复现诊断入口并记录证据，没有据此改变运行时加载预算、存储格式或渲染算法。

## 后续调度优化对照

在上述基线之后加入512在途请求窗口，以及按帧间隔12%/积压20%（最高6ms）的收包预算，复跑相同种子、相机、1280×720、128半径及内存保护：

| 约10秒采样 | 基线 | 优化后 |
| --- | ---: | ---: |
| 客户端收到区块 | 2777 | 5617 |
| Go堆 MiB | 6536 | 3632 |
| 未完成请求 | 16071 | 462 |

优化后17.18秒、9435块、堆6876MiB时触发保护，正常退出保存。两次单次短跑不是统计显著的完整128加载基准；Go堆随GC波动，上表为相近时间快照，不是固定存活内存比较。请求追踪窗口仅约束当前客户端范围，传送时已在服务端排队的请求无法撤销。完整范围的存储成本、绘制成本和长期吞吐仍需进一步处理。基线诊断保留在 work/stress128-before.csv/.cpu，本次结果在 work/stress128.csv/.cpu。
